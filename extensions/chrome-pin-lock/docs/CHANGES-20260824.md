# CHANGES 2026-08-24

## 2026-08-24 13:21 GMT+8 - 二次修订设计文档（逻辑修复 + 安全定位调整）

- **文件**：`docs/chrome-pin-lock-extension-design.md`，修订前版本已备份至 `temp/backups/chrome-pin-lock-extension-design-20260824-132120.md`
- **逻辑修复**：`idleMinutes` 上限 1440 → 240（对齐 `setDetectionInterval` API 4 小时上限）；未设 PIN 引导监听器补 `noPinHintShown` 会话标记实现"仅提示一次"；移除 `onCommitted` 监听（内容闪现 + 双重处理）；`autoLock` 补 `cred` 前置检查；删除死代码 `sessGetAll`
- **匹配修复**：url 前缀改为「尾斜杠归一化 + 边界前缀」匹配，避免 `/admin` 误命中 `/admin-evil`；整体小写化并修正注释矛盾
- **简化（安全定位调整为防君子不防小人的门禁）**：删除 `verifyPinWithMigration` 旧格式迁移死代码；PIN 要求 ≥6 位放宽为 ≥4 位，移除强口令提示；锁页移除 `title` 完整 URL 泄露；`goOptions` 去重绑定
- **文档明确**：解锁粒度为 origin 属有意简化；IP 规则仅支持 IPv4；§8 重写为门禁定位说明

## 2026-08-24 13:09:57 GMT+8 - 重写 chrome-pin-lock 扩展设计文档

- **文件**：`docs/chrome-pin-lock-extension-design.md` 全面改写（804 行，原 479 行），原版已备份至 `temp/backups/chrome-pin-lock-extension-design-20260824-130957.md`
- **manifest 阻塞修复**：`background` 补 `type: module`；删除冗余 `web_accessible_resources`；`options_page` → `options_ui`；新增 `minimum_chrome_version: 102`、`incognito: spanning`、`icons`
- **lib 修正**：`isProtected` 支持 `IP:端口` 精确/通配、domain 去端口路径、URL 前缀归一化、host 小写化；`originKey` 小写化；移除 PBKDF2 二次 SHA-256 直存，新增 `verifyPinWithMigration` 兼容旧哈希
- **background 重构**：新增 `onHistoryStateUpdated`/`onCommitted` 覆盖 SPA；`shouldLock` 前置 `cred` 检查防死锁；`redirectToLock` 加 `tabs.get` 校验与合并写入；`autoLock` 合并 `oldPending`；新增 `tabs.onRemoved` 清理、`storage.session` 降级、`isExtensionUrl` 过滤、`applyIdle` 数值校验与 `onStartup` 清理
- **lock 页重构**：`tabId` 类型归一、`target`/`cred` 为空分支、限流 5 次/30s、`unlocked` Set 去重、`pending` 合并清理、hint 仅展示 origin、`Enter` 快捷键
- **options 页补全**：`normalizeValue`/`validateItem` 校验、去重、删除渲染、PIN 二次确认与 ≥6 位强度提示、`idleMinutes` 1~1440 校验
- **文档增强**：§5 新增 SPA/限流/合规行；§6 测试清单 +6 项、常见坑 +4 项；§7 A2 补充 block 兜底建议；§8 强化 PIN 强度与 host_permissions justification；新增 §10 变更清单

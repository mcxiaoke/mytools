# 2026-08-30

- 17:45 窄滚动条 + 保存自动校验 — `src/Views/ConfigEditorWindow.xaml:7` / `src/Views/TaskEditorWindow.xaml:7` + `src/Views/ConfigEditorWindow.xaml.cs:95` / `src/Views/TaskEditorWindow.xaml.cs:456`：
  - **滚动条**：两编辑器 `ScrollViewer` 垂直滚动条由系统默认 17px 改为 6px 窄条（`Window.Resources` 中 `Style TargetType=ScrollBar` 重写 `Template`，`Track+Thumb` 圆角 `#FFC8C8C8`、`RepeatButton` 透明），右侧不再占宽，与白色卡片边框更协调，`TaskEditorWindow` 与 `ConfigEditorWindow` 保持一致
  - **保存自动校验**：原 `保存`/`保存并应用` 已内置 `Validate()`（`ConfigEditorWindow.xaml.cs:95 DoSave` 先 `Validate(cur)` 失败则 `MessageBox` 阻断、`TaskEditorWindow.xaml.cs:456 OnSaveClick` 聚合校验 `Validate()+重名`），无需单独点 `校验` 即可拦截非法值（`IdleMinutes 0-1440`/`OverlayOpacity 0.3-1.0`/`ExcludeProcesses 非法字符`/`name 重名/cron 非法` 等），`校验` 按钮仅作手动预检保留
  - 验证：`dotnet build -c Debug` 0 警告 0 错误，`TaskEditorWindow` 右侧与 `ConfigEditorWindow` 全列表滚动条均为 6px 细条，`IdleMinutes=9999`/`OverlayOpacity=2` 等非法输入在 `保存` 时自动弹 `校验失败` 并阻止落盘

- 17:30 新增配置编辑器 GUI — `src/Views/ConfigEditorWindow.xaml:1` / `src/Views/ConfigEditorWindow.xaml.cs:1` + `src/App.xaml.cs:431`：
  - **界面**：`540x620` WPF 窗口，`ScrollViewer` 内分 4 区：基础设置（`IdleMinutes` 可编辑下拉 0/1/3/5/10/15/30/60、`ShowClock`、`OverlayOpacity` Slider 0.3-1.0）、系统集成（`AutoStart`/`TasksEnabled`）、排除进程（`TextBox+ListBox` 支持回车添加、逗号分隔、去重、删除）、安全（`PinStatus` 显示 `HasPin` 与哈希掩码、`修改 PIN` 先验旧 PIN 再弹 `FirstRunWindow` 生成新盐哈希，未保存前仅改 `_editing`）、底部路径提示（`ConfigService.FilePath / IsPortableMode`）
  - **逻辑**：`LoadCurrent()` 克隆 `App.Config.Current` 初始化控件，`BuildCurrent()` 聚合表单、`Validate()` 校验 `IdleMinutes 0-1440`、`OverlayOpacity 0.3-1.0`、`ExcludeProcesses` 非法字符，`DoSave(apply)` 经 `CopyTo+Save()` 落盘，`apply=true` 时 `ApplyRuntime()` 同步 `Idle.Threshold/Reset`、`AutoStartService.Sync`、`TaskScheduler.SetGlobalEnabled`、`Controller.ApplyPinFromConfig`、`ProcessExclusionService.InvalidateCache` 并反射刷新托盘；`OnPinChangeClick` 仅改 `_editing` 的 `PinSalt/Hash`（`PinService.GenerateSalt/ComputeHash`），避免未保存即改运行时
  - **集成**：`App.xaml.cs:431` 托盘新增 `配置编辑器...`（`任务` 与 `打开配置目录` 之间），点击 `new ConfigEditorWindow().ShowDialog()` 后 `RefreshMenuChecks/UpdateTrayText/RefreshTaskMenu`，复用 `TaskEditorWindow` 风格
  - 验证：`dotnet build -c Debug` 0 警告 0 错误，窗口可打开，空闲/透明度/自启/任务开关/排除列表/PIN 修改与保存并应用均冒烟通过

- 16:49 样例配置构建时复制，不走代码生成 — `src/Samples/config.sample.json:1` / `src/Samples/tasks.sample.json:1` + `src/ScreenLock.csproj:24` + `src/Services/Tasks/TaskConfigService.cs:44` / `src/Services/ConfigService.cs:50`：
  - **新增样例文件**：`src/Samples/config.sample.json`（含 `IdleMinutes/AutoStart/ShowClock/OverlayOpacity/PinSalt/PinHash/TasksEnabled/ExcludeProcesses` 全字段注释，`ExcludeProcesses` 示例 `["GenshinImpact.exe", "eldenring", "qbittorrent.exe"]`）与 `src/Samples/tasks.sample.json`（与原 `BuildSampleJson()` 内容一致，含 `startup/manual/hotkey/watch/interval/daily/when+模板` 7 类注释示例，`docs/AUTORUN-DESIGN.md` 同步）
  - **构建时自动复制**：`ScreenLock.csproj:24` 新增 `<None Include="Samples\*.sample.json" Link="*.sample.json"><CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory></None>`，`dotnet build -c Debug/Release` 后 `bin/*/net48/config.sample.json`（1185B）与 `tasks.sample.json`（2467B）与 `ScreenLock.exe` 同目录，用户可直接重命名为 `config.json/tasks.json` 使用，无需手写
  - **不再代码生成**：`TaskConfigService.cs:44` 新增 `TryReadSampleFile()` 优先从 `exe/tasks.sample.json`（或 `Samples/tasks.sample.json`）读取样例内容复制到 `tasks.json` 与 `tasks.sample.json`（`LoadOrCreate` + 补写分支），缺失时才回退 `BuildSampleJson()` 兜底；`ConfigService.cs:50` 新增 `ConfigSamplePath` + `EnsureSampleCopied()`，每次 `LoadOrCreate` 后若 `DirPath/config.sample.json` 缺失则从 `exe/config.sample.json` 拷贝一份供参考，`AppSettings` 默认值保持代码侧 `Merge` 校验不变
  - 验证：`dotnet build -c Debug` / `Release` 均 0 警告 0 错误，`bin/Debug/net48` 与 `bin/Release/net48` 均含两份 `*.sample.json`，与源码 `Samples/` 内容一致（UTF-8），首次启动无 `tasks.json` 时自动从样例复制生成已冒烟通过

# 2026-08-29

- 13:54 新增 `ScreenLock.slnx:1`（VS2026 新格式，对标 `C:\Home\Projects\pwgen\dotnet\pwgen.slnx:1`）：
  - 内容：`<Solution><Project Path="src/ScreenLock.csproj" /></Solution>`，位于 `tools/screenlock/` 根，与 `Directory.Build.props` 同级，`dotnet sln ScreenLock.slnx list` → `src\ScreenLock.csproj`
  - 选型：VS2026 (18.9.2) + dotnet 10.0.400 原生支持 `.slnx`（XML 可读、合并友好），`dotnet build ScreenLock.slnx -c Debug/Release` 与 `devenv ScreenLock.slnx` 均验证 0 警告 0 错误，版本机制输出正常；如需兼容旧版 VS/Rider 可再 `dotnet new sln` + `dotnet sln migrate` 生成传统 `.sln`
  - 验证：`dotnet build ScreenLock.slnx -c Debug` → `0.1.0-debug`，`dotnet build ScreenLock.slnx -c Release` → `0.1.0-20260829-bd96be9`，`devenv` 可直接打开

- 13:52 引入统一版本号机制（copy 自 `C:\Home\Projects\pwgen\dotnet\Directory.Build.props:1`）：
  - 新增 `Directory.Build.props:1`（`Major=0 Minor=1 Patch=0 VersionPrefix=0.1.0 Epoch=2026-01-01 Product=ScreenLock`，`GenerateCustomVersion` Target 在 `GetAssemblyVersion` 前注入）：Debug `Version=0.1.0-debug AssemblyVersion=0.1.DaysSinceEpoch.0 FileVersion=0.1.DaysSinceEpoch.HHmm`，Release `Version=0.1.0-yyyyMMdd-gitshort AssemblyVersion/FileVersion=0.1.0.0`，`InformationalVersion=Version`，`AssemblyMetadata(GitCommit/BuildTime/DaysSinceEpoch)` 注入，`git rev-parse --short HEAD` 失败回退 `unknown`，`DaysSinceEpoch=today-Epoch`
  - `src/ScreenLock.csproj:12` 清理：移除硬编码 `<Version>0.1.0</Version>` / `<Product>`，改由 `Directory.Build.props` 统一管理（`GenerateAssemblyInfo=true` 保持不变，`Message` 输出 `=== [ScreenLock 编译信息] Version=... ===`）
  - 验证：`dotnet build -c Debug` → `Version=0.1.0-debug AssemblyVersion=0.1.240.0 FileVersion=0.1.240.1351 InformationalVersion=0.1.0-debug GitCommit=bd96be9 BuildTime=2026-08-29`；`dotnet build -c Release` → `Version=0.1.0-20260829-bd96be9 AssemblyVersion=0.1.0.0 FileVersion=0.1.0.0`，`FileVersionInfo` 与 `AssemblyMetadata` 反射均正确

- 13:31 ScreenLock 排除进程（防游戏挂机误锁）— `docs/DESIGN.md:46`/`docs/USAGE.md:99`：
  - **配置**：`Models/AppSettings.cs:9` 新增 `ExcludeProcesses: List<string> = []`（`CopyTo:30` 同步，`Merge` 默认空），`Services/ConfigService.cs:125` 读写扩展：`ExtractStringArray`/`SerializeStringArray` 支持 `["game.exe","notepad.exe"]` 数组与 `"a.exe, b.exe"` 单字符串兼容，大小写不敏感，`Save` 持久化，`ReadFile:110` 手动解析弥补 `SimpleJson` 无法处理数组
  - **检测**：新增 `Services/ProcessExclusionService.cs:1`（`Normalize` 去路径/`\.exe`/空白转小写，`CheckRunning` 比对 `Process.GetProcesses().ProcessName`，2s 缓存 `CacheMs=2000` + `InvalidateCache`），`App.xaml.cs:114` `ShouldSuspendIdle()` 新增 `ProcessExclusionService.IsExcludedRunning(ExcludeProcesses)` 分支（与 `_sessionLocked/_pauseUntil/IsSystemBusy` 并列），命中则冻结 `IdleDetector._effectiveMs`，`ReloadConfig:155` 失效缓存
  - **文档**：`USAGE.md:99` 新增 §12 排除进程示例与规则，`DESIGN.md:46` 配置示例与 §3.2 空闲检测说明更新
  - 验证：`dotnet build -c Release` 0 警告 0 错误；`explorer/notepad` 等常驻进程命中 `IsExcludedRunning` 为真、空列表/不存在为假，`config.json` 数组/逗号字符串往返解析与 `Reload` 生效均冒烟通过

- 11:20 Tier1+2 全量落地（`docs/USAGE.md:1` 新增实用说明，`docs/AUTORUN-DESIGN.md:1` 升至 v0.4）：
  - **新触发器**：`Models/TaskDefinition.cs:6` 新增 `Manual/Hotkey/Watch`，`Services/Tasks/Triggers/ManualTrigger.cs:1`（托盘手动）、`HotkeyService.cs:1`+`HotkeyTrigger.cs:1`（`RegisterHotKey` 支持 `Ctrl+Alt+Q` 等）、`FileWatcherTrigger.cs:1`（`FileSystemWatcher` 防抖 500ms，`filter/event` 可配）
  - **条件与模板**：`TaskCondition/Evaluator.cs:1`（`when.onlyIdle/acPower/networkAvailable/fileExists/fileNotExists`，`ExecuteAsync` 前判定，失败记 `skipped(condition)`），`TemplateExpander.cs:1`（`{{date/time/yyyyMMdd/task/scripts}}` 等，`TaskRunner.cs:22` 对 `file/args/workDir` 同时 `ExpandEnvironment+Template`），`Options.notifyOnFailure` 默认开，`TaskSchedulerService.RecordFailureNotify` 经 `App.ShowBalloonPublic` 气泡并入 `最近运行`
  - **调度集成**：`TaskSchedulerService.cs:187` 扩展 `CreateTrigger`、`GetManualTasks/RunManual/GetRecent/AddRecent`，全局开关、`Reload` 同步 `TasksEnabled` 并 `Apply` 新触发器
  - **托盘动态菜单**：`App.xaml.cs:30` 新增 `_manualMenu/_recentMenu`，`RefreshTaskMenu()` 动态列出 `manual` 任务（点击即 `RunManual`）、`最近运行` 10 条、`任务编辑器...` 入口；`ShowBalloonPublic` 供后台失败通知
  - **轻量编辑器**：`Views/TaskEditorWindow.xaml(.cs):1`（`900x620`，左侧列表 `新增/复制/删除`，右侧按触发器类型动态显隐表单，`浏览` 默认 `scripts/`、`scripts/ 快速选择` 下拉自动填工作目录，`校验` 调 `Validate()` 并阻断重名，`保存` 经 `TaskConfigService.Save()` 格式化写回，`保存并重载` 调 `TaskScheduler.Reload+RefreshTaskMenu`）
  - **配置增强**：`TaskConfigService.cs:641` 示例更新至含 `manual/hotkey/watch` 与 `when/模板`，新增 `Save()` 序列化；`Build Sample` 同步更新
  - 验证：`dotnet build -c Release` 1 警告 0 错误（仅 `FileWatcher isFile` 未使用），裸名脚本 `hotkey/watch/manual` 均已冒烟，编辑器校验与自动填目录正常

- 10:35 改动 1-3 落地（`src/Services/Tasks/ScriptResolver.cs:1` 新增）：
  - **脚本路径自动解析**：`bat/cmd/ps1/js/py` 等若 `action.file` 未指定绝对路径，自动在 `<DirPath>/scripts/` 中查找（便携：`exe/scripts/`；非便携：`%AppData%\ScreenLock/scripts/`），`TaskRunner.cs:30` 引入 `ScriptResolver.ResolveScriptPath` + `EnsureScriptsDir`，`TaskConfigService.cs:34` 自动创建 `scripts/`，示例 `tasks.json` 更新为裸文件名（`hello.js/sync.bat/monitor.py`）。
  - **js/py 自动用系统 PATH 的 node/python 执行**：`ScriptResolver.FindNode/FindPython` 遍历 `PATH/PATHEXT` 查找 `node.exe/python.exe/python3.exe/py.exe`，`TaskRunner.cs:64` 新增 `.js→node "file"`、`.py/.pyw→python -u "file"` 包装，所有脚本类保持 `CreateNoWindow=true` 无黑窗口，`FileName/Arguments` 日志含解析后完整路径与 `workDir`。
  - **任务总开关（默认启用）**：`Models/AppSettings.cs:8` 新增 `TasksEnabled=true` 并持久化到 `config.json`（`Services/ConfigService.cs:88` 读写），`Services/Tasks/TaskSchedulerService.cs:13` 新增 `_globalEnabled/IsGlobalEnabled/SetGlobalEnabled`（禁用时 `StopTriggers` 清空触发器，启用时 `Load+Apply` 重建），`src/App.xaml.cs:30` 新增托盘 `任务 → 启用任务调度`（`CheckOnClick` 默认 `Config.Current.TasksEnabled`，`CheckedChanged` 调 `SetGlobalEnabled+Config.Save` 并气泡），`ReloadTasks` 同步勾选态，`TaskSchedulerService.Reload` 同步外部对 `config.json` 的修改。
  - 验证：`dotnet build -c Release` 0 警告 0 错误；裸名 `hello.js/hello.py/hello.ps1/hello.bat` 经 `scripts/` 解析+`node/python/powershell/cmd` 包装后执行，`logs/task-*.log` 捕获 stdout/stderr 正确，`TasksEnabled` 切换持久化并控制触发器启停已通过冒烟测试。

- 10:30 新增 AutoRun 计划任务模块（独立于锁屏，仅复用常驻壳）— 详见 `docs/AUTORUN-DESIGN.md`：
  - **配置**：`%AppData%\ScreenLock\tasks.json`（便携模式则 `exe 同目录/tasks.json`），与 `config.json` 同目录判定；不存在时自动生成带注释的 `tasks.sample.json` 模板。支持 `startup/interval/daily/cron/sessionLock/sessionUnlock/idle` 7 种触发器，`every: "1h30m"` / `everySec` / `at: "HH:mm"` / `expr: "0 9 * * 1"` 等写法，`name` 唯一性校验、注释 `//`/`/* */` 容错。
  - **执行**：`Services/Tasks/TaskRunner.cs` 以 `UseShellExecute=false + CreateNoWindow=true + WindowStyle.Hidden + RedirectStdOut/Err` 无窗口执行，自动包装 `.ps1→powershell -NoProfile -ExecutionPolicy Bypass -File`、`.bat/.cmd→cmd /c`、`.vbs→wscript`，环境变量展开、`workDir` 自动推导、超时 `timeoutSec` 通过 `taskkill /T /F` 杀树。
  - **日志**：`Services/Tasks/TaskLogger.cs` 输出到 `logs/tasks.log`（汇总）+ `logs/task-<name>.log`（单任务 stdout/stderr + 调度记录），按 `task-xxx.log` 分文件，单文件 5MB 轮转保留 3 档，UTF-8 追加，`lock` 并发安全。
  - **调度**：`Services/Tasks/TaskSchedulerService.cs` + `Triggers/*`（Startup/Interval/Daily/Cron/SessionEvent/Idle），`DispatcherTimer` 驱动，`idle` 复用/独立 `IdleDetector`，`SystemEvents.PowerModeChanged(Resume)` 5s 后补检 `daily/cron`，`allowConcurrent=false` 时并发跳过记 `skipped`，失败 `retry` 重试，异常隔离不影响宿主。
  - **集成**：`src/App.xaml.cs:21,88` 新增 `TaskScheduler` 生命周期（`Start` 于 `Idle` 之后、`Stop/Dispose` 于 `OnAppExit`），`Services/ConfigService.cs:34` 新增 `TaskFilePath/LogsDirPath`，托盘新增 `任务` 二级菜单（重载任务/编辑 tasks.json/打开 logs 目录），`ReloadTasks()` 气泡提示生效数/错误数。
  - **模型**：`src/Models/TaskDefinition.cs`（含 `CronHelper` 5 字段校验与匹配、`ParseDuration/TryParseTime`）。
  - 编译：`dotnet build -c Release` 0 警告 0 错误 `ScreenLock -> bin/Release/net48/ScreenLock.exe`，`TaskRunner` 隐藏执行与日志捕获已通过 `runner-test/ps-test` 冒烟验证。

# 变更记录

## 2026-08-25 18:36 — 修复锁屏透明度叠加导致遮罩过透

- `LockWindow.xaml`：`RootBorder.Background` 由 `#E00B0F14` 改为 `#FF0B0F14`（不透明）
- 原因：背景自带 alpha ≈0.88，与 `OverlayOpacity`（默认 0.88）相乘，实际不透明度仅约 0.77，调到 0.9 也只有约 0.79
- 现在透明度完全由设置项 `OverlayOpacity` 单独控制，设为 1.0 即完全不透明


# CHANGES-20260914

## WebUI 工具栏操作按钮基线对齐与条目计数换行修复（2026-09-14 14:47 GMT+8）

- **工具栏按钮基线与高度完全统一**：
  - 排查并修复「上传文件」（`<button class="upload-btn">`）、「新建文件夹」（`<button class="tb-btn">`）与「打包下载」（`<a class="tb-btn">`）因标签属性与 Pico CSS 默认 `margin-bottom` 差异导致的垂直高低错位问题。
  - 将所有工具栏按钮统一归一化为 `.tb-btn` / `.upload-btn` 样式规范：`height: 32px; line-height: 1; margin: 0 !important; padding: 0 12px;`，共享统一的高对比度卡片背景、细边框、圆角（`var(--fl-radius)`）与悬停态。
  - 保留无 `!important` 的 `display: inline-flex`，确保 Alpine.js `x-show` 原生内联 `display: none` 可安全隐藏根目录未开启的操作。
- **视图切换与条目数量文字换行修复**：
  - 排查修复右侧视图切换器 `.view-ctrl` 因带有 `role="group"` 触发 Pico CSS 的 `width: 100%` 导致挤占整行空间、将紧邻的条目计数「5 项」挤压为两行的问题。
  - 为 `.view-ctrl` 显式设置 `width: auto !important; max-width: fit-content !important; margin: 0 !important;`，自适应内容紧凑布局。
  - 为条目计数 `#itemCount` / `.item-count` 增添 `white-space: nowrap !important; flex-shrink: 0 !important;`，彻底杜绝小屏及宽屏下的字符换行。
- **自动化测试与视觉真机双重验收**：
  - Playwright 全量 36 项 E2E 自动化测试 100% 通过（桌面与移动 400x840 视口零横向滚动溢出）。
  - 导出桌面端及手机端渲染截图并比对验收确认无误。

## 扩展特性：音视频播放、文本编辑、目录创建、重命名、ZIP 打包、二维码与安全删除（2026-09-14 14:02 GMT+8）

- **音视频内联播放（Audio/Video Player）**：
  - 复用 `/raw/...` 支持 HTTP 206 Range 分段请求流式播放，拖动进度条即开即播。
  - 模态弹窗内自适应播放 `<video>`（高画质视频控制条）与 `<audio>`（精致暗黑黑胶唱片旋转动效及歌曲名称/元数据展示），点击遮罩或 `Esc` 退出并自动暂停。
- **文本 / 代码 / Markdown 在线查看与保存**：
  - 支持 `.txt`, `.md`, `.json`, `.yaml`, `.yml`, `.go`, `.js`, `.py`, `.sh`, `.css`, `.html` 等文本源码。
  - 等宽排版、行数/字节动态统计、支持 `Tab` 键缩进插入 2 个空格、`Ctrl+S` / `Cmd+S` 快捷保存。
  - 前端脏状态变动追踪，未保存退出时弹窗确认及 `window.beforeunload` 防误触拦截。
  - 后端通过临时文件（`.tmp`）落盘并 `os.Rename` 原子覆盖替换，防止写入中断损坏原始文件，保存后自动增量更新内存索引。
- **目录创建（Mkdir）**：
  - 工具栏提供「📁 新建文件夹」按钮，支持弹出模态框输入新目录名。
  - 后端防路径遍历安全校验，自动更新内存索引并在前台实时刷新显示。
- **直链复制与移动端扫码（Direct Link & QR Code）**：
  - 列表与网格卡片提供直链复制按钮，点击自动写入系统剪贴板并弹出 Toast 提示。
  - 内置嵌入轻量纯前端 SVG 二维码生成库（~20KB `qrcode.min.js`），离线免 CDN 依赖，手机直接扫码即可在移动设备浏览下载。
- **目录流式打包 ZIP 下载**：
  - 工具栏及目录行操作支持「📦 打包下载」，后端基于 `archive/zip` 边读取磁盘边流式写入 HTTP 响应，服务端零临时文件和零磁盘空间占用。
  - 打包文件名使用虚拟路径基名（如 `/data` 下载为 `data.zip`），内部自动跳过排除的敏感文件及系统隐藏文件。
- **文件与文件夹重命名**：
  - 支持文件和子目录重命名，后端自动执行同名冲突校验、敏感排除项阻断与合法性校验，并同步更新内存索引。
- **独立口令安全删除（Safe Delete & Delete Token）**：
  - 针对删除危险操作，在 `config.yaml` 中新增独立的 `manage.deleteToken` 配置（与全局访问 `token` 解耦）。
  - 删除确认模态框采用醒目危险红色警示，强制输入删除口令，支持 SessionStorage 临时会话记忆；未配置 `deleteToken` 时只要开启管理即可直接确认。
  - 后端采用 `subtle.ConstantTimeCompare` 恒定时间比对防时序攻击；严格禁止删除根挂载点（如 `/data`），执行删除后同步更新索引。
- **细粒度管理权限控制**：
  - 配置文件新增 `manage:` 模块，支持全局开关 `enabled`、全局只读 `readOnly`，以及细粒度控制 `allowEdit`、`allowDelete`、`allowMkdir`、`allowRename`、`allowZip` 及 `maxTextSize`（默认 2MB）。
- **全套测试保障与体验校验**：
  - 扩展后端单元测试，覆盖文本存取、目录新建、重命名、安全删除拦截与验证、ZIP 流式压缩等用例。
  - 扩展 Playwright E2E 自动化测试（新增 `media.spec.js` 和 `manage.spec.js`），全套 9 大测试套件共 36 项用例 100% 通过。
  - 桌面端与移动端（400x840）全面兼顾，无水平溢出，操作按键精致排布。

## 服务端启动日志输出版本与构建元数据（2026-09-14 11:44 GMT+8）

- **启动日志补全**：在 `src/main.go` 初始化日志系统后，增加 `logger.Info("starting FileList v%s (%s, built %s)", version, gitCommit, buildTime)` 输出，确保在后台服务运行模式及写入独立日志文件（如 `/var/lib/filelist/filelist.log`）时，能够作为首条日志清晰记录当前服务的版本号、Git Commit Hash 及编译时间。
- **CLI 命令行版本参数**：新增 `-version` 与 `-v` 命令行参数支持，便于终端快速查看二进制详细构建版本。
- **验证与部署**：通过 `deploy.ps1` 交叉编译并热重启远程服务器，查看 `filelist.log` 验证确认启动日志正常打出。

## Web 页面页脚与构建元数据（Git Commit & 构建时间）呈现（2026-09-14 11:42 GMT+8）

- **后端构建元数据捕获与注入**：
  - 在 `src/main.go` 中新增 `initBuildInfo()`，优先通过编译期注入参数 `-ldflags "-X main.gitCommit=... -X 'main.buildTime=...'"` 接收元数据；未传参时自动降级使用 Go 1.18+ 标准库 `runtime/debug.ReadBuildInfo()` 读取 VCS 版本信息（`vcs.revision`、`vcs.time`、`vcs.modified`）。
  - 在 `src/server.go` 中将 `gitCommit` 和 `buildTime` 动态渲染进 `window.__FILELIST_CONFIG__`、页脚占位符以及 `/api/stats` 统计接口中。
- **构建与部署脚本自动化**：
  - 更新 `build.ps1` 与 `deploy.ps1`，自动提取当前工作区精确短 Git Hash（带 `-dirty` 工作区变动标记）及本地真实时间戳（`yyyy-MM-dd HH:mm:ss`）并通过 `-ldflags` 注入编译二进制。
- **WebUI 极简页脚设计**：
  - 在 `index.html` 底部新增 `<footer class="site-footer">`，居中低饱和呈现服务版本号、等宽字体 Commit Hash 及构建时间。
  - 在 `app.css` 中适配明暗模式与移动端自适应（小屏自动缩小字号与换行排版），不干扰页面滚动与内容区域。
- **测试与部署全量验证**：
  - 新增 Go 单元测试 `TestPageHTML_BuildInfo` 与 `TestHandleStats_BuildInfo`。
  - 新增 Playwright E2E 自动化测试用例，全套 27 项测试 100% 通过。
  - 成功编译并部署至 `root@192.168.1.118:8787`，实机验证页面与 `/api/stats` 正常输出。

## WebUI Lightbox 缩放自适应与交互修复（2026-09-14 11:31 GMT+8）

- **4K 缩放顶部操作栏溢出修复**：排查并修复在页面使用字号缩放（`document.body.style.zoom >= 1.3`）时，因视口单位 `100vh`/`100vw` 与 CSS `zoom` 叠加导致全屏遮罩尺寸放大为 150% 并垂直居中，使得顶部操作栏（文件名、计数条与关闭按钮）被推挤至屏幕上方视口外（`y: -258px`）无法看见和点击的问题。重构弹窗布局为百分比弹性盒模型（`width: 100%; height: 100%; position: fixed; inset: 0;`），并在图片容器上使用 `max-height: 100%`，无论字号缩放为多少或在 4K 宽屏下，顶部操作栏均稳固定位在屏幕顶部。
- **全区域点击关闭与手势兜底**：
  - 点击全屏任意空白背景区域、图片两侧或图片上下空白处均可立即关闭 Lightbox。
  - 右上角关闭按钮强化高亮醒目样式（红底透明微光），增大触摸热区。
  - 保留并修复键盘 `Esc` 键退出，以及 `← / →` 翻页箭头。
- **静态资源版本号缓存消除（Cache Busting）**：
  - 在 `index.html` 引入静态资源时追加动态编译版本号参数 `app.js?v=...` 与 `app.css?v=...`。
  - 后端 `/static/` 路由缓存策略调整为 `Cache-Control: no-cache`，避免跨端更新时移动端与桌面端浏览器因 24 小时强缓存而载入旧版 JS/CSS 脚本。

## WebUI 图片网格（Grid）模式与全屏 Lightbox 预览（2026-09-14 11:22 GMT+8）

- **双视图无缝切换**：工具栏右侧新增视图切换控件（`[ ☰ 列表 | ⊞ 网格 ]`），支持一键在传统表格列表与图片卡片网格之间平滑切换，状态通过 `localStorage`（键 `filelist_view_mode`）自动跨页面与刷新记忆，默认保持列表模式保证 100% 兼容。
- **响应式 CSS Grid 布局**：
  - 采用 `repeat(auto-fill, minmax(150px, 1fr))` 智能自适应列宽，4K 宽屏自适应 10+ 列，手机竖屏（400x840）优雅呈现 3 列，无横向滚动条溢出。
  - 图片缩略图使用 `aspect-ratio: 1 / 1` 正方形配合 `object-fit: cover` 居中裁剪，卡片带悬浮上浮动效与精细 1px 边框。
  - 文件夹与常规文件优雅降级：文件夹显示大目录图标并支持点击直接下钻进入，普通文件展示对应扩展名大图标。
- **原生懒加载与零后端依赖**：
  - 网格模式下图片采用浏览器原生 `loading="lazy"` 属性，只有滑入视口附近的缩略图才会触发网络加载，超大图库首屏极速加载。
  - 直接复用既有 `/raw/...` 直链，完美利用 Go 标准库的 304 缓存机制。
- **全屏沉浸式 Lightbox 预览**：
  - 点击图片卡片唤起全屏深色毛玻璃遮罩预览弹窗（`backdrop-filter: blur(8px)`）。
  - 顶部栏展示当前文件名、图片序号计数（如 `1 / 4`）、原图下载与新标签页打开操作。
  - 支持键盘快捷键控制：`Esc` 退出大图、`← / →` 左右方向键循环无缝切换上一张/下一张图片。
- **端到端自动化测试覆盖**：
  - 新增 `tests/e2e/specs/grid.spec.js`，引入 `C:\Home\Temp\Jigsaw_Organized\Animals` 真实图片作为测试夹具。
  - 覆盖视图持久化、网格卡片渲染、缩略图懒加载、全屏弹窗、键盘方向键切换及手机视口无溢出等场景，全套 26 项 E2E 用例 100% 通过。

## WebUI 对比度修复、设计变量规范化与全站视觉体验调优（2026-09-14 11:05 GMT+8）

- **字号指示器对比度修复**：修复 Pico CSS v2 在 `<button>` 内部将 `--pico-color` 强制定义为纯白反色（`var(--pico-primary-inverse)`）导致在亮色背景下字号重置按钮（`#fontReset` / `.font-val`）出现“白底白字”无法看清的问题。重构字号控制器样式，改用显式语义文本色 `--fl-text` 并强化加粗（`font-weight: 600`），亮色模式下呈现深深灰色（`#1e293b`），暗黑模式下呈现浅白色（`#f1f5f9`）。
- **控制器分界线与边框修复**：针对 Pico CSS v2 缺乏 `--pico-border-color` 变量导致边框丢失的问题，统一引入独立可控的设计令牌 `--fl-border`，为字号缩放器中间的百分比按钮补充左右两道清晰的物理分隔边框（`border-left` / `border-right`），并为首页按钮、搜索框、字号调节器及表格容器增添精细 1px 边框与微阴影。
- **全站明暗模式对比度与视口视觉检查**：
  - 修复 CSS 选择器语法错误，全面兼顾手动暗黑模式类与系统 `prefers-color-scheme: dark` 媒体查询。
  - 清理表格列单元格的背景干扰（`td { background: transparent !important; }`），确保整行 Hover 高亮在所有列均匀呈现。
  - 经桌面端与移动端（400x840）真机渲染截图确认：字号缩放指示、搜索交互栏、面包屑路径及表格列表在明暗双模式下对比分明、排版稳固，全量测试与实机部署验证通过。

## WebUI 路线 A 重构：Pico.css + Alpine.js 极简免编译嵌入架构（2026-09-14 10:59 GMT+8）

- **技术栈全面升级**：采用轻量免编译（Zero-Build）架构，引入官方预编译压缩库：
  - **Pico.css v2**：纯语义化 CSS 框架，自带高质感现代设计、优雅暗黑模式自适应及响应式布局基础。
  - **Alpine.js v3**：声明式响应式框架，彻底告别以前 500+ 行手写原生 DOM 拼接（`innerHTML += ...`），以 `x-for`、`x-show`、`x-model` 实现清晰的数据驱动渲染。
- **后端资源组织与嵌入升级**：
  - `src/server.go` 升级为 `//go:embed web/*` 目录级嵌入，依旧保持单个独立二进制分发优势，完全脱离外部网络与公网 CDN。
  - 新增 `/static/` 高速静态托管路由，配置 `Cache-Control: public, max-age=86400` 缓存头，跨目录导航与页面刷新零网络往返延迟。
- **业务代码清晰分层解耦**：
  - `src/web/static/pico.min.css` & `src/web/static/alpine.min.js`：第三方标准库资产。
  - `src/web/static/app.css`：FileList 专属的主题色、移动端双行响应式布局与组件细化样式。
  - `src/web/static/app.js`：`filelistApp` Alpine 组件，封装路径解码、历史记录导航、目录/根挂载点加载、防抖搜索与 `<mark>` 高亮、多字段排序、字号缩放持久化、流式上传及全局拖拽上传监听。
  - `src/web/index.html`：极简纯语义 HTML 模板，代码量缩减至仅 150 行。
- **全量测试与实机验证**：全量 22 项 Playwright 端到端（E2E）测试用例 100% 通过（耗时 13.3s），并通过 `deploy.ps1` 部署至 `192.168.1.118:8787` 实测验证正常。

## WebUI 移动端 (400x840) 响应式适配、字号调节与一键部署（2026-09-14 10:43 GMT+8）

- **移动端两行式顶部导航布局**：针对小屏幕（<= 640px，特别是 400x840 手机屏幕），采用 CSS Flexbox `order` 属性将原本在同一行的导航元素重排为上下两行：
  - 第一行：左侧放置「📁 根目录」按钮（`order: 1`），右侧通过 `margin-left: auto` 放置字号缩放控制器（`order: 2`），两者并排仅占约 50% 宽度，即使放大字体也绝不换行挤压。
  - 第二行：搜索框独占整行（`order: 3; width: 100%`），提供充足的输入空间，搜索按钮和清除按钮布局平稳。
- **清除按钮位置调整**：将搜索框清除按钮 `[✕]` 调整至「搜索」按钮左侧，避免点击清除时触发搜索按钮跳动抖动。
- **基准字号与缩放控制器**：
  - 基准字号提升至 16px，对应等比调优图标尺寸、表格间距与字阶比例，改善 4K 等高分屏下的可读性。
  - 顶部增加字体缩放调节器（`[ A- ] [ 100% ] [ A+ ]`），范围支持 85% ~ 200%，基于 `localStorage` 跨刷新自动持久化。
- **表格列宽与截断优化**：表格增加 `th.actions-col` 样式控制，文件名称项引入 `.name-text` 文本溢出省略截断（`text-overflow: ellipsis`），确保超长文件名不会撑破移动端视图宽度。
- **E2E 自动化测试覆盖**：在 `navigation.spec.js` 中新增移动视口（400x840）与放大缩放测试用例，校验双行布局对齐及整个视口无水平滚动条（`scrollWidth <= clientWidth`）；并在 `run-e2e.ps1` 中加入源码变动自动检测与智能重新编译机制。
- **一键部署脚本**：新增 `deploy.ps1`，自动跨平台交叉编译 `linux/amd64` 二进制、SCP 上传至目标主机并通过 `install -m 755` 规避正在运行进程锁，最后平滑重启 `filelist.service`。

## E2E 浏览器自动化测试套件（2026-09-14 10:01 GMT+8）

- **测试框架与浏览器驱动**：引入 Playwright 并适配本机系统 Microsoft Edge（Chromium），无需重复下载大体积 Chromium 二进制，运行开销小、启动极快。
- **服务生命周期自动管理**：编写 `test-server.js` 与 `fixtures.js`，实现动态端口分配、测试夹具目录初始化、临时配置文件生成、服务进程拉起与 `/api/stats` 就绪检查，并在测试结束时优雅终止与自动清理。
- **全场景端到端覆盖**：编写 20 项端到端浏览器用例，全面覆盖导航浏览（挂载点、子目录、面包屑、空目录）、前端排序（名称/大小/时间）、实时防抖搜索与高亮跳转、文件下载与预览、文件上传与同名冲突编号自动生成、拖拽上传、敏感文件排除拦截、Token 鉴权等核心交互流程。
- **一键运行脚本**：编写 `tests/run-e2e.ps1`，支持自动检测构建状态与 npm 依赖并一键执行全部 E2E 测试。

## 文件上传功能与同名冲突自动后缀编号（2026-09-14 09:42 GMT+8）

- **后端流式上传**：新增 `POST /api/upload` 接口，使用 `r.MultipartReader()` 与 `io.Copy` 将文件直接流式写入磁盘，支持局域网大文件传输并保持极低内存占用。
- **同名防覆盖策略**：文件落盘时检测同名冲突，自动以 `(1)`、`(2)` 等数字编号生成不冲突的新文件名，保障已有文件不被覆盖。
- **跨平台文件名安全与截断**：新增 `sanitizeFilename` 与 `truncateUTF8`，自动过滤/替换不同操作系统下的非法字符（`<>:"/\|?*` 与控制字符替换为 `_`），避开 Windows 保留设备名称（`CON`、`PRN`、`AUX`、`NUL`、`COM1-9` 等前缀 `_`），清除尾部点和空格，超长文件名在有效 UTF-8 边界截断并保留扩展名及冲突后缀预留空间。
- **目录安全与防越界**：限制仅在具体子目录下允许上传（根挂载点列表禁止直接上传），严格防路径穿越，校验目标目录有效性与 `withinRoot` 越界检测，自动过滤 `excludeFiles` 敏感文件。
- **前端 SPA 交互**：工具栏增加上传按钮，支持文件多选与页面拖拽上传，配合 `XMLHttpRequest` 实时展示上传进度百分比，上传成功后自动刷新目录列表。
- **配置项开关**：新增 `upload.enabled` 配置项（默认 `false`），保持默认只读的安全性。
- **工具函数解耦重构**：将与路由和状态无关的纯函数及通用辅助函数（路径越界校验、JSON响应、文件名安全清理、UTF-8安全截断、同名编号生成等）从 `server.go` 抽取至独立的 `utils.go`，对应单元测试归入 `utils_test.go`。

# 控制台可见性记录（由 scripts/verify.ps1 生成）
# 生成时间: 2026-09-16 13:40:03 +08:00
# 目标: C:\Home\Projects\mytools\tools\better-updater\target\release\updater.exe
# 说明: 判定【无继承 std 句柄 + 真实控制台】下输出是否可见（RELEASE-READINESS P1-5）。

# 状态: exit=0
=== 控制台可见性（无继承 std 句柄 + 真实控制台）===
  [PASS] --help 的输出可见（退出码 0）
  [PASS] --version 的输出可见（退出码 0）
  [PASS] 未知参数的错误可见（退出码 2）
  [PASS] 缺少 --target 的错误可见（退出码 2）
  [PASS] --dry-run 被拒的原因可见（退出码 2）
  [PASS] --dry-run 的计划清单可见（退出码 0）
[OK] 6/6 用例全部在控制台上可见；被重定向/管道捕获时的语义另由 cargo test 覆盖

# 变更记录 2026-09-15

## 10:05 (GMT+8) 参数单位契约规范化与两代更新器全面对齐

- **参数单位与默认值规范化（`--max-uncompressed`）**：
  - `rust/src/args.rs`：`--max-uncompressed` 明确规范为接收整数 MiB（默认值 `4096` 即 4 GB），内部换算为字节数，彻底去除大数字传参歧义；
  - `rust/README.md` & `docs/rust-updater-architecture-design-lite.md`：文档参数表同步更新为 `--max-uncompressed <MiB>`；
  - Cargo 测试 5/5 全部通过。
- **与 better-updater 兼容性评估与平替落地**：
  - 新增 `docs/updater-vs-better-updater-compatibility.md` 深度对比分析文档；
  - 驱动 `better-updater` 落地 `--gui` / `--gui-title` 原生 Win32 进度条及双语自适应支持，完成全量兼容性闭环。

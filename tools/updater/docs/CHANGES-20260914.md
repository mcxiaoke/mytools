# 变更记录 2026-09-14

## 19:07 (GMT+8) 全量同步架构文档与项目 README (v1.3.0)

- **架构文档升级**：
  - `docs/rust-updater-architecture-design-lite.md` 升至 **v1.3.0**：
    - 完整增补 §8 原生 Win32 极简更新窗口与区域自适应机制（独立 UI 线程防假死、双模式动效、关闭按钮防护、`GetUserDefaultUILanguage()` 零依赖双语切换、Tier 2 降级隔离）；
    - 更新 §9 模块划分（引入 `gui.rs` 与 `strings.rs`）与依赖版本规格（`lexopt 0.3.2`, `windows-sys 0.61.2`, `zip 8.6.0`, `sha2 0.11.0`）；
    - 同步 §11 方案对比矩阵，标定 ~366 KB 单文件无外部 GUI 依赖规格。
- **使用指南与项目首页同步**：
  - `rust/README.md`：更新核心特性、编译体积（~366 KB）、参数契约（`--gui`, `--gui-title` 动态自适应）、新增 §3.5 GUI 架构与双语机制详解；
  - `README.md`：根目录索引同步 Rust 版特性状态（零额外依赖、可选原生 GUI、中英文自适应）。

## 19:04 (GMT+8) 落地集中式文案管理与 Win32 原生区域自动切换 (zh-CN 中文 / 其余全英文)

- **文案与国际化集中化**：
  - 新建 `rust/src/strings.rs`：集中统一定义全部 GUI 界面文案（标题、准备、等待进程、校验包、文件更新中、完成拉起），彻底消除分散在各模块中的硬编码字符串；
  - **原生区域检测（零额外依赖）**：调用 Win32 `GetUserDefaultUILanguage()` 识别用户界面首选语言：
    - 若当前语言为简体中文大陆（`LANGID == 0x0804` / `zh-CN`），自动呈现中文文案；
    - 其它所有语言和区域（英文、繁体中文、日文、德文等），一律自动平滑 Fallback 至标准英文文案；
    - 若主程序通过命令行传入自定义 `--gui-title`，则优先遵循用户自定义标题。
  - **产物体积**：完全零依赖（仅引入 `windows-sys` 的 `Win32_Globalization` 特性），Release 体积依然保持在 **366 KB**（374,784 字节，体积净增 0 字节）。
- **自动化测试**：
  - 新增 `strings::tests::test_locale_fallback` 单元测试，覆盖 `zh-CN`、`en-US`、`en-GB`、`zh-TW`、`ja-JP`、`de-DE` 映射；
  - Cargo 测试 5/5 全部通过，Real Host E2E 30/30 全部通过。

## 18:59 (GMT+8) 新增零外部依赖原生 Win32 极简更新对话框与全面中文/Unicode 验证

- **新特性实现**：
  - `rust/src/gui.rs`：新增基于原生 Win32 API (`user32.dll` / `comctl32.dll` / `gdi32.dll`) 的极简进度对话框模块，零外部 Rust 第三方库引入：
    - **独立 UI 线程**：创建专属消息循环 (`GetMessageW`)，大包解压与耗时操作期间窗口永不假死（杜绝“未响应”）；
    - **双模式进度动画**：等待进程与校验阶段自动启用原生跑马灯 (`PBS_MARQUEE`)，解压替换阶段无缝切换为实时百分比进度条 (`0% ~ 100%`)；
    - **清晰排版与安全防护**：自动加载微软雅黑/系统平滑字体，禁用替换期间的窗口关闭按钮 (`SC_CLOSE`) 避免用户误杀导致半成品；
    - **Tier 2 降级隔离**：无桌面环境或窗口创建失败自动静默降级，不影响核心更新事务。
  - `rust/src/args.rs`：新增可选参数 `--gui`（开启对话框）与 `--gui-title`（自定义标题，默认“正在更新应用...”）。
  - `rust/src/transaction.rs` & `rust/src/main.rs`：打通逐文件解压与原子替换的进度回调管道。
  - **二进制体积**：仅 **366 KB**（仅比原纯 CLI 版增加 28 KB），继续保持极致轻量。
- **中文与全 Unicode 支持全链路验证**：
  - 命令行参数通过 `GetCommandLineW` 原生 UTF-16 解析，参数与标题支持任意中文；
  - 路径经 `to_verbatim_path` 规范化为 `\\?\` Verbatim UTF-16 传递至 Win32 内核，完美支持中文目录与空格路径；
  - `temp/test_chinese_and_gui.py` 自动化测试通过（中文路径、中文包内文件名、中文标题、中文 keep 规则全部 100% 通过）。
- **验证与测试**：
  - 真实宿主 E2E 30 项回归测试：30/30 全部通过 (100%)。
  - Go verify 兼容测试：26/26 全部通过 (100%)。
  - 桌面真实运行交互演示：`temp/run_gui_demo.py` 执行成功。

## 18:45 (GMT+8) 编写并交付项目级与 Rust 更新器使用指南 README

- **文档产物**：
  - `rust/README.md`：详细的宿主应用集成与使用指南（涵盖核心特性、CLI 参数契约、多语言启动示例【Rust / Go / Flutter(Dart) / C# / Electron】、Detached 进程分离与工作目录关键实践、自更新临时副本方案、`.updatekeep` 打包规范与故障排查）。
  - `README.md`：更新仓库根入口索引，明确指向 Rust 极简原生版与 Go 历史版本文档及指南。

## 18:12 (GMT+8) 采纳评审意见升级 lite 架构至 v1.2.0 并完成真实可执行文件 E2E 全套验证

- **文档升级**：`docs/rust-updater-architecture-design-lite.md` 升级至 **v1.2.0**：
  1. 诚实界定工程边界：明确声明接受强杀（`taskkill /f` / 进程树强杀）导致的不一致风险（换取极简无 WAL），修正“100%可靠”措辞。
  2. 补齐内部保留名强拦截：`.updater_tmp`、`.updater_bak`、`.updater.lock`、`.updater/` 禁止入包（预检直接拒），杜绝备份树篡改漏洞。
  3. 补全自更新局限与提权继承说明：自身在 target 内不自更新；`--elevate` 提权拉起的主程序继承管理员权限。
  4. 重试预算微调至 20 次 × 500ms（10秒 Defender 窗口），新增 `--max-uncompressed`（默认 4GB）与 Zip bomb 压缩比防范。
- **代码实施**：
  - `rust/src/preflight.rs`：落地内部保留名强拦截（fail-closed）与 Zip bomb 检查。
  - `rust/src/args.rs`：默认参数调优至 20 次 × 500ms，新增 `--max-uncompressed`。
  - `rust/examples/mock_host.rs`：构建测试用真实 Windows 宿主应用程序（编译产出 v1.0.0 与 v2.0.0 二进制）。
  - `temp/test_e2e_real_host.py`：构建真实可执行文件 E2E 测试套件，覆盖真实宿主触发更新、保留名攻击拦截、中途文件锁冲突自动回滚、Zip Slip 拦截、自身在 target 避让、Zip bomb 防护。
- **验证结果**：
  - 真实宿主 E2E 测试：30/30 全部通过（100% 通过率）。
  - 原 Python verify.py 兼容测试：26/26 全部通过（100% 通过率）。
  - Cargo 单元测试：3/3 全部通过。

## 18:04 (GMT+8) 极简版 Rust 自动更新器 (updater-rs lite) 完成代码实施与全量验证

- **产物目录**：`rust/`（标准 Cargo 工程，产物 `updater.exe` 单文件仅 336 KB）。
- **技术实现**：
  - 严格依据 `docs/rust-updater-architecture-design-lite.md` (v1.1.0) 实施，剔除所有过度异常处理。
  - 核心实现：
    - `args.rs`：基于活跃维护的 `lexopt` 库实现完整命令行参数解析。
    - `winutil.rs`：实现 `\\?\` Verbatim 长路径规范化、目录写权限探测、`ShellExecuteExW` UAC 提权防递归、单实例 `.updater.lock` 独占写锁。
    - `process.rs`：`OpenProcess` 句柄绑定等待（防 PID 重用）与 `CreateProcessW` 显式传递 `target` 工作目录。
    - `keep.rs`：`.updatekeep` 清单规则匹配（目录/精确/glob）与自身路径（`target 内`）避让。
    - `preflight.rs`：ZIP 格式校验、SHA-256 计算、Zip Slip 严格防御、磁盘剩余空间检查与 `--require` 校验。
    - `transaction.rs`：`ReplaceFileW` 原子覆盖 + 备份、瞬时锁 15 次重试、内存回滚栈（`rollback_stack`）异常还原。
    - `main.rs`：线性生命周期调度、错误日志记录与失败兜底拉起。
- **验证结果**：
  - Cargo 单元测试：3/3 全部通过。
  - Python 端到端集成测试（`verify.py`）：26/26 全部通过（100% 通过率）。
  - 恶意 Zip Slip 拦截测试：在预检阶段即被完全拦截，退出码 1，无磁盘污染，兜底拉起正常。
  - `--dry-run` 测试：准确输出变更与跳过清单，退出码 0。

## 18:00 (GMT+8) 架构文档按职责拆分：docs/project/（9 份，源文档冻结只读）

- **背景**：v4.3 单文档 2242 行，一份文件同时承担"设计基线 / 评审记录 / 改动日志 / 决策考古"四职，读者被迫在正文里做减法。
- **原则**：**源文档一字未动**（`docs/rust-updater-architecture-design-v4.2.md`，2242 行 / 211677 字节，mtime 保持 17:46）；只做归位与去重，**不删任何决策、数值、错误码、不变量与测试用例**。
- **产物**（`docs/project/`，合计 2443 行，最大文件 536 行）：
  - `README.md` 95 —— 索引、一页纸十条决策、按角色阅读顺序、章节映射、考古归档指引
  - `SCOPE.md` 140 —— **新增的边界页（v4.3 缺失）**：首发包含/排除（含重新纳入的触发条件）、**止损线**、复杂度硬规则、明确不做的事
  - `DESIGN.md` 536 —— 前提与原地替换论证、内部状态存放位置、术语、启动序列、无损预检、签名闭环、保护语义与路径安全
  - `TRANSACTION.md` 449 —— 三层恢复与 Journal、不变量 I1–I10、`--recover`、事务替换、回滚算法、提交收尾、版本回退（暂缓）
  - `RUNTIME.md` 339 —— 句柄等待、RM 只读诊断、UAC 提权/降权、影子 Worker、自清理与运行期目录、拉起主进程与进度
  - `CONTRACT.md` 231 —— CLI 参数、退出码、模式矩阵、`.updatekeep` 与打包约定、Go 版差异清单、迁移与回退
  - `PLAN.md` 284 —— 复杂度约束、依赖与体积、模块划分与日志、Phase 0–8、DoD
  - `TESTING.md` 299 —— 测试矩阵 A–F、CI 守卫脚本、故障注入钩子形态、包清单生成脚本
  - `REFERENCE.md` 70 —— Win32/Deflate 事实核验、先例调研来源、被推翻的评审意见
- **新增强化**：首次把"首发范围"与"终态能力清单"切开；首次给出作用于首发的止损线（**首发 Tier 0 ≤ 1200 行**，区别于终态的 2600 行）；CI 守卫建议整体推迟到 Phase 4 之后（治理成本高于被治理对象）。
- **验证**：全部跨文件章节引用经脚本校验通过（9 份文件、0 处断链）。

## 17:57 (GMT+8) 精简版架构文档升级：v1.0.0 → v1.1.0 (Implementation Ready)

- **文件**：`docs/rust-updater-architecture-design-lite.md`
- **定位**：不增任何新功能，纯粹修复 5 个 Win32 工程实施暗坑，确保可无歧义对照编码：
  1. 明确 `ReplaceFileW` 替换前必须递归创建 `dst`、`tmp`、`bak` 的父目录（避免内核报 3 路径不存在）。
  2. 显式规定 `CreateProcessW` 的 `lpCurrentDirectory` 必须设为 `target` 绝对路径（杜绝丢失工作目录导致主程序找不到同级 DLL 闪退）。
  3. 调整锁获取时序至 UAC 提权判定之后，消除受限目录下因无权写锁文件导致的假锁死与提权失效。
  4. 规范 ZIP 目录项与文件项的分流处理，回滚栈补齐空目录逆序清理 `RemoveDir`。
  5. 明确 Win32 API 调用时统一使用 `\\?\` Verbatim 长路径前缀，免疫 Windows 260 字符限制。

## 17:52 (GMT+8) 新增极简高可用版架构设计文档：docs/rust-updater-architecture-design-lite.md

- **背景**：针对 v4.2/v4.3 出现的架构过度设计与分析瘫痪问题，保持 4.2 文档原样不动，提取其真正优秀的 Win32 可靠性机制，剔除假想敌异常处理。
- **产物**：创建独立设计文档 `docs/rust-updater-architecture-design-lite.md`。
- **要点**：
  - 砍掉断电 WAL 事务日志、看门狗套娃监控、世代备份 GC、Restart Manager 诊断、复杂公钥基础设施。
  - 保留并规范 Win32 句柄绑定进程等待、ReplaceFileW 原子替换 + 内存回滚表、Zip Slip 防御、.updatekeep 清单、自身避让与防递归提权等硬核可靠性机制。
  - 目标代码量收敛至 500~700 行 Rust。

## 17:45 (GMT+8) updater 架构设计文档：v4.2 → v4.3（三方评审合并修订）

**文件**：`docs/rust-updater-architecture-design-v4.2.md`（版本头已升为 **v4.3.0**，文件名保持不变）
**备份**：`temp/backups/rust-updater-architecture-design-v4.2.md.20260914-172809.bak`
**合并记录**：`docs/temp/rust-updater-review-verification-20260914.md`（三份评审的逐条核验 + 最终裁定 + 改动映射）

### 变更摘要（只列重点，细节见文档 §0.5）

- **新增章节**：§0.5 三方评审合并总览（含采纳 34 项、两处技术裁定、未采纳项）；§3.3 模式与必填参数矩阵；§17.5 故障注入钩子形态。
- **删除能力**：`--splash`（全链路：参数/依赖/用例/附录）；`--rm-shutdown` 清场分支（Restart Manager 仅保留只读诊断，feature 粒度修正）。
- **暂缓能力**：`--rollback-previous` 移出首发（`previous/` 保留 + `_meta.txt` 仍在 Phase 4）；§19 节奏与 §18 兜底路径同步调整。
- **高优先级缺陷修正**：
  - 事务循环补 **三处 `MkdirAll`**（dst / backup / tmp 的父目录）——原文档只有 dst 一处。
  - 提权分支补 `!--elevated-worker` 守卫，**杜绝"提权后仍不可写 → 无限 UAC 递归"**。
  - `_meta.txt` 规格补全：必须在删除 Journal 之前写入，`ADDED` 只取权威 `NEW` 集合；`restore_from(source_dir, excludes)` 排除契约写明。
  - §6.6 步骤 3/4 与铁律 I3 的矛盾修正（失败时保留 Journal 并跳过错删）。
  - zip bomb 比值阈值 1000:1 → **2000:1**（原值低于 Deflate 物理上限 1032:1，会误杀合法包），权威层只留绝对字节上限。
  - 锁错误码显式映射：**32 与 5（`DELETE_PENDING`）均进轮询**；保留 `FILE_FLAG_DELETE_ON_CLOSE`（不采纳"改普通锁文件"的建议）。
  - 分身路径取消"先关锁再派生"，消除 50–300ms 无主窗口。
  - 看门狗：安全超时命中时**不得**判为"新实例接管"（改为记 WARNING + 标注降级 L3）；补齐参数透传集合；`--delete-zip` 在该分支不执行。
  - §5.4 `--recover` 的应用启动自检由"推荐"升为**硬性集成契约**（"任意时刻确定性收敛"承诺的前提）。
  - §10.3 Session 0 禁止降级拉起（避免以 System 在无桌面会话静默启动应用）。
  - §13.3 `to_verbatim()` 补四条前置规范化（绝对化 / 分隔符统一 `\` / 消除 `.`、`..` / 去尾随空格与点）；保持单一路径，不做长度条件化。
  - §13.1 保留名拦截补**裸 `.updater`**（精确名 + 前缀两条规则）。
  - §6.4 明确哈希复核的**被校验集合 = 计划内实际写入的文件**（否则 `.updatekeep` 类"随包发布但永不覆盖"的文件会让自检必然失败）。
  - §7.3 版本比较写死为可测规则（含预发布后缀）；`MIN_UPGRADABLE_FROM` 补消费者；§5.2.3 Journal `TARGET` 比对先规范化。
  - 流程图重画：修 mermaid 节点 `P` 重复定义（原图"写权限预检"会被渲染成"派生看门狗"）、`--dry-run` 短路提到取锁之前。
- **工程治理**：Tier 0 预算 1600 → **2600 行产品代码（WARN）**；依赖口径 6 → **7 个直接依赖**；CI 守卫脚本重写（`cargo metadata` 计数、去注释后匹配禁用构造、测试代码单列）；默认重试预算 `10×300ms` → `20×500ms`。
- **测试矩阵**：改名 3 条、拆分 1 条、删除 2 条（改为"参数已删除"回归）、新增 12 条。

### 未做（需后续拍板）

Phase 0 六项实构建/实测（含 `\\?\` 前缀下四个核心 Win32 调用）；`--rollback-previous` 是否在后续版本引入；锁语义的最终确认（若实测发现 5 的窗口有实际影响再评估）。
